<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Цвет фона -->
    <stroke android:width="1dp"
    android:color="@color/black" /> <!-- Цвет рамки -->
    <padding
        android:bottom="7dp"
        android:left="10dp"
        android:right="3dp"
        android:top="7dp" /> <!-- Отступы внутри рамки -->
</shape>